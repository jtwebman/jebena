package java.lang;

// Minimal placeholder: Jebena has a single VM class registry; ClassLoader exists
// so signatures that mention it (e.g. Proxy.newProxyInstance) resolve.
public abstract class ClassLoader {
    protected ClassLoader() {}
}

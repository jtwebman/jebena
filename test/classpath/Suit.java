package cp;

// Exercises lazy classpath loading of an enum (java.lang.Enum + getEnumConstants
// + Class mirror) from a directory/jar, not just plain classes.
public enum Suit {
    CLUBS, DIAMONDS, HEARTS, SPADES
}
